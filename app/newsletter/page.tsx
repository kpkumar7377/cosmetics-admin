import NewsletterClient from "./NewsletterClient";

export const metadata = {
  title: "Newsletter & Campaigns | Admin Atelier",
};

export default function NewsletterPage() {
  return <NewsletterClient />;
}
